services:
  go-app-integration-test:
    profiles:
      - app_integration_test

  go-app:
    profiles:
      - app

  go-app-health-check:
    profiles:
      - app_post_check

  db:
    profiles:
      - db

  migrate:
    profiles:
      - migrate
