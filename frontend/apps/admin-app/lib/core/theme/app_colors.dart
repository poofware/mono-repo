import 'package:flutter/material.dart';

class AppColors {
  // Primary color used throughout the app
  static const Color primary = Colors.blue; // Change this to your desired color

  // Secondary color (optional)
  static const Color secondary = Colors.grey;

  // Background color (optional)
  static const Color background = Colors.white;

  // Text colors (optional)
  static const Color primaryText = Colors.black;
  static const Color secondaryText = Colors.black;

  // Button colors (optional)
  static const Color buttonBackground = Color(0XFF57B9FF); // Matches WelcomeButton
  static const Color buttonText = Colors.white;
}